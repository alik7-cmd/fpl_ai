version: '3.8'

services:
  fpl-ai:
    build: .
    container_name: fpl-ai
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
      - /app/__pycache__
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
